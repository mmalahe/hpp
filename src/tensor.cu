#include <hpp/tensorCUDA.h>

namespace hpp
{
#ifdef HPP_USE_CUDA

// Chose to go with header-only, because explicit instantiations are a pain
// if they involve the dimensions as well

#endif /* HPP_USE_CUDA */
}//END NAMESPACE HPP